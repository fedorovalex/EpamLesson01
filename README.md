Ќаписать функцию, котора€:
- возвращает количество заданных символов в строке;
- определ€ет, что в массиве строк есть одинаковые элементы;
- возвращает перемножение двух матриц.
- возвращает пересечение массивов double.